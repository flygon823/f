# Meshy でダウンロードした .glb を ここに置く

GitHub の画面で このフォルダを開いて **Add file → Upload files** から のせてください（ブランチは `claude/intelligent-heisenberg-8ncqi7`）。
ファイル名は なんでも大丈夫です（例：`komugi.glb`・`momiji.glb`・`chapu.glb`）。

のせたら、`village/tools/prep-model.mjs` で かるくして `public/models/` に入れます（くわしくは `village/README.md` の「住民を Meshy AI の立体モデルにする」）。
ここに置いた元のファイルは 島では使いません。
